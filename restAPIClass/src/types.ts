export type Customer = {
    id: number;
    name: string;
    address: string;
    email: string;
}

export type Product = {
    id: number;
    name: string;
    description: string;
    price: number;
}